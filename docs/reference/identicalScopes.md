# Check if two objects have the same BEID scope

Check if two objects have the same BEID scope

## Usage

``` r
identicalScopes(x, y)
```

## Arguments

- x:

  the object to test

- y:

  the object to test

## Value

A logical indicating if the 2 scopes are identical
