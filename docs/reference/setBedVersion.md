# Feeding BED: Set the BED version

Not exported to avoid unintended modifications of the DB. This function
is used when modifying the BED content.

## Usage

``` r
setBedVersion(bedInstance, bedVersion)
```

## Arguments

- bedInstance:

  instance of BED to be set

- bedVersion:

  version of BED to be set
